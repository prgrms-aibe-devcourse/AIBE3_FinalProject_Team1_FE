# Components

재사용 가능한 컴포넌트를 관리하는 폴더입니다.

## 구조

- `ui/` - 기본 UI 컴포넌트 (버튼, 입력, 카드 등)
- `layout/` - 레이아웃 컴포넌트 (Header, Footer, Sidebar 등)
- `features/` - 기능별 컴포넌트 (필요시 추가)

## 사용 예시

```tsx
import { Button } from "@/components/ui/button";
import { Header } from "@/components/layout/header";
```

