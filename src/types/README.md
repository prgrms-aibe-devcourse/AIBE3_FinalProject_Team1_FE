# Types

TypeScript 타입 정의를 관리하는 폴더입니다.

## 구조

- `api.ts` - API 관련 타입
- `domain.ts` - 도메인 모델 타입

## 사용 예시

```tsx
import type { ApiResponse } from "@/types/api";
import type { User } from "@/types/domain";
```
