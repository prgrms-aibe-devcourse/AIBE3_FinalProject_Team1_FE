# Lib

라이브러리 설정 및 유틸리티 함수를 관리하는 폴더입니다.

## 구조

- `utils/` - 유틸리티 함수 (formatDate, debounce, throttle 등)
- `constants/` - 상수 (API URL, 환경 변수 등)
- `validations/` - 검증 로직 (필요시 추가)

## 사용 예시

```tsx
import { formatDate } from "@/lib/utils/date";

import { API_URL } from "@/lib/constants/api";
```
